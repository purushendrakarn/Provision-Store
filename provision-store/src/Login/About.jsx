import React from 'react'

const About = () => {
  return (
    <div>
    
In the "service.jsx" file located within the "src" folder, the OAuth API is seamlessly retrieved. Upon successfully matching the header and data with a response status of 200, the process proceeds by resolving and appending pertinent information to the "login.jsx" file. Subsequently, the retrieved data undergoes encoding in SHA-256 before initiating navigation to the product list. This entire sequence ensures a secure and validated login experience for users.
    </div>
  )
}

export default About
